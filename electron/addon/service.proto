syntax = "proto3";
package anytype;
option go_package = "pb";

import "basic.proto";
import "wallet.proto";


service Anytype {
    rpc _GenerateMnemonic (GenerateMnemonic) returns (Empty);
}


service AnytypeClient {
    rpc _PrintMnemonic (PrintMnemonic) returns (Empty);
}
