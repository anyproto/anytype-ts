# constant/ - Application Constants

Static configuration values used throughout the app. Imported from `src/json/` files.

Contains re-exports and additional constant definitions for delays, limits, default values, and configuration that doesn't change at runtime.
