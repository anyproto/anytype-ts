import * as React from 'react';
import { observer } from 'mobx-react';

const SidebarObject = observer(class SidebarObject extends React.Component {
	
    render() {

        return (
			<React.Fragment>
			</React.Fragment>
		);
    };

});

export default SidebarObject;