import { registerIcon } from '../../registry';
import Error from './error';
import Syncing from './syncing';

registerIcon('chat/messageStatus/error', Error);
registerIcon('chat/messageStatus/syncing', Syncing);
