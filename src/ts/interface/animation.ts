/**
 * @fileoverview Contains the enum "AnimType" and related definitions.
 */
export enum AnimType {
        Normal   = 0, // Standard animation
        Text     = 1, // Text-related animation
};

export enum AnimDirection {
        From     = 0, // Animate from state
        To               = 1, // Animate to state
};